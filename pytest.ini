[pytest]
testpaths = tests
addopts = --cov=app --cov-report=term-missing --cov-fail-under=70
markers =
    unit: testes unitários sem dependências de infra (banco, Kafka, Redis)
    integration: testes que usam infra simulada (SQLite, fakes de Kafka)
